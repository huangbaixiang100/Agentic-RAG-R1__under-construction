# Token级奖励分配系统

## 🎯 概述

Token级奖励分配系统是对现有GRPO训练的重要改进，实现了**细粒度的奖励分配机制**，让每个问题获得与其信息增益相匹配的奖励。

### 核心改进

- **问题级奖励分配**：每个问题根据其Shapley信息增益获得不同奖励
- **过程+结果奖励**：结合信息获取过程和最终答案正确性
- **Token级精确分配**：奖励分配到具体的token上，提供精确的信用分配
- **完全兼容**：保持与现有系统的完全兼容性

## 🔄 奖励计算机制

### 传统模式 vs Token级模式

| 维度 | 传统模式 | Token级模式 |
|------|----------|-------------|
| **奖励范围** | 全局奖励 | 问题级奖励 |
| **分配方式** | 平均分配 | 基于贡献分配 |
| **信用分配** | 粗粒度 | 细粒度 |
| **激励机制** | 结果导向 | 过程+结果导向 |

### 奖励计算公式

```python
# 每个问题的奖励
问题奖励 = α × Shapley增益 + β × Shapley增益 × 答案正确
最终答案奖励 = γ × 答案正确

# 其中：
# α: 过程奖励权重 (默认1.0)
# β: 结果奖励权重 (默认1.0) 
# γ: 最终答案奖励 (默认3.0)
```

## 🚀 快速开始

### 1. 使用预配置脚本

```bash
# 启用Token级奖励训练
./script/training/train_token_level_reward.sh
```

### 2. 自定义参数训练

```bash
accelerate launch ./hhhdoctor_train.py \
    --use_shapley=True \
    --use_token_level=True \
    --alpha_reward=1.0 \
    --beta_reward=1.0 \
    --gamma_reward=3.0
```

### 3. 兼容性训练（保持原有方式）

```bash
# 传统训练，完全兼容
./script/training/train_hhhzero2_shapley.sh
```

## ⚙️ 参数配置

### 主要参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `use_token_level` | False | 是否启用token级奖励分配 |
| `use_shapley` | False | 是否使用Shapley值加权 |
| `alpha_reward` | 1.0 | 过程奖励权重 |
| `beta_reward` | 1.0 | 结果奖励权重 |
| `gamma_reward` | 3.0 | 最终答案奖励权重 |

### 推荐配置

```bash
# 1. 平衡模式（推荐）
--alpha_reward=1.0 --beta_reward=1.0 --gamma_reward=3.0

# 2. 强调过程模式
--alpha_reward=2.0 --beta_reward=0.5 --gamma_reward=3.0

# 3. 强调结果模式
--alpha_reward=0.5 --beta_reward=2.0 --gamma_reward=3.0
```

## 📊 使用场景

### 适用情况

✅ **推荐使用Token级奖励**
- 多轮医疗问诊对话
- 需要引导模型问出高价值问题
- 要求精确的信用分配
- 过程和结果同等重要的任务

✅ **继续使用传统模式**
- 单轮问答任务
- 主要关注最终结果准确性
- 计算资源有限的情况

### 实际效果示例

```
医疗对话示例：
问题1: "患者主要症状是什么？" 
  → 获取关键信息"胸痛2小时"
  → Shapley增益=0.7，问题奖励=1.4

问题2: "还有其他症状吗？"
  → 获取一般信息"头晕"  
  → Shapley增益=0.3，问题奖励=0.6

最终诊断: "急性心肌梗死，选择A"
  → 答案正确，最终奖励=3.0

总奖励分配更合理，高价值问题获得更多奖励
```

## 🛠️ 技术实现

### 核心函数

1. **`compute_token_level_rewards()`** - 计算token级奖励分配
2. **`extract_question_boundaries()`** - 识别问题token边界  
3. **`compute_question_shapley_gains()`** - 计算问题Shapley增益
4. **`compute_token_level_advantages()`** - 计算token级advantage

### 系统架构

```
输入对话 → 问题边界识别 → Shapley增益计算 → 问题奖励计算 → Token分配 → GRPO更新
```

## 🔍 监控和调试

### 训练日志示例

```
🎯 使用token级奖励分配模式
📊 Token奖励统计 - 均值: 0.8542, 标准差: 1.2341
🔍 Shapley整体评分模式（旧版本方式）
📊 总事实数: 6, 前50%事实数: 3
```

### SwanLab监控

系统会自动记录以下指标：
- Token级奖励分布
- 问题级Shapley增益
- 过程vs结果奖励比例
- 训练收敛情况

## 🔄 兼容性说明

### 向后兼容

✅ **完全兼容现有代码**
- 不启用`use_token_level`时，行为与原系统完全一致
- 现有训练脚本无需修改即可运行
- 评估和推理代码无需更改

### 升级路径

1. **第一阶段**：保持现有训练不变
2. **第二阶段**：启用Shapley加权 (`use_shapley=True`)
3. **第三阶段**：启用Token级奖励 (`use_token_level=True`)

## 🎉 预期效果

### 训练改进

- **更精确的信用分配**：高价值问题获得更多奖励
- **更好的引导效果**：模型学会问出重要问题
- **过程和结果平衡**：同时优化问诊过程和诊断准确性
- **训练稳定性提升**：细粒度奖励减少噪声

### 性能提升

- 问诊质量提升：模型问出更有价值的问题
- 信息获取效率提升：更快获取关键信息
- 诊断准确性提升：更好的信息整合能力

